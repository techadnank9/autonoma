{
    "name": "@autonoma/image",
    "version": "0.0.1",
    "description": "Image processing utilities for screenshots, bounding boxes, and colors.",
    "type": "module",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "typecheck": "tsc --noEmit",
        "lint": "oxlint --fix src/",
        "fmt": "oxfmt --write src/"
    },
    "dependencies": {
        "@autonoma/errors": "workspace:*",
        "@autonoma/logger": "workspace:*",
        "sharp": "^0.33.5"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "typescript": "catalog:"
    }
}
